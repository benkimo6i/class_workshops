Guess the Number: ExpressJS Edition

Includes a form to submit POST requests &
views (both EJS & Pug versions) to render the application.

You should be able to run:
$ npm install

to automatically install all dependencies.
